{
  "name": "@openclaw/plugin-unraid",
  "displayName": "Unraid",
  "description": "Manage your Unraid server — Docker containers, VMs, array, shares, system info, and more.",
  "version": "0.1.0",
  "configSchema": {
    "serverUrl": {
      "type": "string",
      "description": "URL of the OpenClaw Connect server (e.g., http://192.168.1.100:9876)",
      "required": true
    },
    "apiKey": {
      "type": "string",
      "description": "API key for authenticating with OpenClaw Connect",
      "required": true,
      "secret": true
    }
  }
}
